"""CHARGE: charge door / socket on the car's left rear quarter.

STUB. One thin box flush with the body side.
"""

import common


def build_charge(ctx):
    return common.stub_box_block(ctx, "CHARGE")
